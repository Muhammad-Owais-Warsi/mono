## mono
A simple and lightweight note app.


### keyboard guide
- `ctrl-k`: open cmdk window
- `ctrl-n`: open a new note.
- `ctrl-shift-n`: open new note in a split window
- `ctrl-arrow(left/right)`: to switch active pane
